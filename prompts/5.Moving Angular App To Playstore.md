### Orientation Prompt

"We're wrapping our Angular PWA as a native Android app with
Capacitor so we can publish to the Play Store. Read CLAUDE.md
and our build setup — we build to the docs folder for GitHub
Pages. Confirm you understand our build output before we add
Capacitor, because Capacitor needs to know where our built
web app lives."

### Add Capacitor

"Add Capacitor to the project and walk me through what it
installs and configures — I want to understand the native
project it creates, not just run commands. Point our Capacitor
config at our existing production build output so it wraps
the app we already build. Make sure to use latest best practice
and do use Kotlin instead of Java"

### Run the Wrapped App

"Build our Angular app for production, sync it into the
Capacitor Android project, and walk me through running it
on my phone or an emulator. I want to see our existing app —
voice, receipts, backup — running inside the native shell
before we change anything."

### Swap In Native OCR

"Now the payoff from Episode 5. We isolated receipt OCR behind
a single method that returns raw text from an image. I want to
swap the Tesseract implementation for a native on-device OCR
plugin (ML Kit) now that we're in Capacitor — better accuracy,
still free, still on-device.

Research the right Capacitor OCR plugin, then replace ONLY
the inside of our OCR method. The camera, parser, and review
screen must stay exactly as they are. Propose the plugin
and the change before making it."

### The Play Store Release

"Walk me through setting the app's native identity — the
package/application ID, the app name 'Expense Tracker', and
the native app icons and splash screen using our CodeKin gold-
on-black branding. Tell me what icon assets and sizes Android
needs."

### The Release Build

"Build the signed release bundle — the .aab file Google Play
needs. Walk me through the command and confirm the output is
correctly signed and ready to upload.

### The Play Store Listing

"I already have a Google Play Console account. Walk me through
publishing THIS app:

- creating the new app entry in the console
- the store listing assets I need (screenshots, description,
  feature graphic)
- the content rating and data-safety questionnaire — we use
  the camera and Google Drive, so help me declare that
  honestly: receipts processed on-device, backup in the user's
  own Drive, no central server, no data collected by me
- check the CURRENT production-release requirements for my
  account type (there may be testing-track prerequisites) so
  I know the real path to going live
- uploading the signed .aab and submitting"
