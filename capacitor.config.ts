import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cck.expensetracker',
  appName: 'Expense Tracker',
  webDir: 'dist/expense-tracker/browser'
};

export default config;
